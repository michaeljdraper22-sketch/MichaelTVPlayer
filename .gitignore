# Python
__pycache__/
*.pyc
.venv/

# PyInstaller build output
build/
dist/

# Personal data — never upload
TV Recordings/
*.ts
*.mp4
*.mkv

# PyInstaller staging (rename-swap builds)
build_staging/
dist_staging/

# CCExtractor's OCR engine writes this wherever it runs (dev + dist)
tess.log

# Private bundled VLC runtime (full install copy, ~185 MB) — never commit
/vlc/

# PyInstaller staging (exe rebuild workpath)
build_pyinstaller/

# ZCode agent session data
.zcode/
/android/michaeltv_core/
/android/.p4a/
/android/bin/
/android/.buildozer/

# android-gradle (native Gradle/Chaquopy build) — toolchain + build output
/android-gradle/.tools/
/android-gradle/.gradle/
/android-gradle/build/
/android-gradle/app/build/
/android-gradle/local.properties
/android-gradle/.idea/
*.jks
/android-gradle/keystore.properties
